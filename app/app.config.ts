export default defineAppConfig({
  ui: {
    colors: {
      // Doxa brand: forest green primary, sage secondary, beige neutral.
      primary: 'forest',
      secondary: 'sage',
      neutral: 'beige'
    }
  }
})
